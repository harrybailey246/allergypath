DATABASE_URL="postgresql://ehr:ehr@localhost:5432/ehr"

# Auth0 / JWT validation settings
AUTH0_ISSUER_BASE_URL="https://YOUR_AUTH0_DOMAIN"
AUTH0_AUDIENCE="https://YOUR_AUTH0_DOMAIN/api"
# Optional: override if your tenant uses a different custom claim for roles
AUTH0_ROLE_CLAIM="https://example.com/roles"

# Default clinic used when provisioning new users on first login
DEFAULT_CLINIC_ID="default-clinic"
DEFAULT_CLINIC_NAME="Default Clinic"
